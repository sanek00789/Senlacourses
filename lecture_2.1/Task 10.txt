String[] ArrayWord = new String[] {'Lets', 'learn', 'something', 'new', 'today'};
    for (Integer i = 0; i < ArrayWord.size(); i++) {
        if (math.mod(i, 2) == 0) {
            System.debug(ArrayWord[i]);
        }    
    }
    