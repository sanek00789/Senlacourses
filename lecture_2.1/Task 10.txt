String phrase = 'Lets learn something new today';
String [] arrayWords = phrase.split(' ');
    for (Integer i = 0; i < ArrayWords.size(); i++) {
        if (math.mod(i, 2) == 0) {
            System.debug(ArrayWords[i]);
        }    
    }
    